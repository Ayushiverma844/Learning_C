//Wap to accept values from the user in 2D array integer arrary of size 3*4,and display their sum and avg.

#include<stdio.h>
int main()
{
    int i,j,sum=0;
    int arr[3][4];
    for(i=0;i<3;i++)
       {
           for(j=0;j<4;j++)
           {
               printf("enter no ");
               scanf("%d",&arr[i][j]);
               sum=sum+arr[i][j];
           }
       }
       printf("sum is %d",sum);
       printf("\navg is %f",(float)sum/12);
    
       return 0;
}